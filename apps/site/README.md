# @relay/site

Marketing site plus the `/docs` route. One app, one deployment (Q10, settled).

Split docs out only if it needs versioning and search badly enough to justify a second
deployment and a second set of shared-component friction.
