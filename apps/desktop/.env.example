# The shell loads the renderer from here. In production this becomes a local bundle.
RELAY_UI_URL=http://localhost:5173

# Open DevTools on launch. Unset by default — the stock menu's Toggle Developer Tools
# (Cmd+Opt+I on macOS, Ctrl+Shift+I elsewhere) opens it when you want it.
# RELAY_DEVTOOLS=1
