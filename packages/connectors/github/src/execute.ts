// Execution against the provider, using the invoker's Pipes token resolved at point of use.
//
// BROKER ONLY. apps/runtime must never import this — enforced by lint in the root
// eslint config. If that rule ever fires, the fix is to move the work, not the rule.

export {};
