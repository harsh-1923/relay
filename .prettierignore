node_modules/
dist/
build/
coverage/
pnpm-lock.yaml

# Generated by drizzle-kit and rewritten on every `db:generate`. Formatting it would
# produce a diff on every run and conflicts on every branch. It is still checked in —
# the snapshots are what `generate` diffs against.
supabase/migrations/
.wrangler/

# Vendored by `npx skills add`. Upstream's formatting — reformatting it would make every
# skill update a noisy diff.
.agents/skills/

# Generated by `pnpm diagrams` — 30kB of JSON each, reviewed as a diff of build.mjs instead.
docs/diagrams/*.tldr
